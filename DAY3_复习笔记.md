# Day 3 复习笔记：前端 × HTTP 联调

> **个人定制版** · 基于你 2026-09-10 的实操 + 2026-09-11 的 20 题自测结果生成
>
> **自测成绩：15 / 20（合格，需复习）**
> 失分**不是零散知识点**，而是**两个错误的思维模型**在重复扣分（见第二章、第三章的 ⚠️ 专项）。先把那两个翻转过来，分数立刻上去。
> 实操部分（4 个 TODO + 5 处故障注入）全部独立完成，动手能力没问题。

---

## 掌握情况总览

| 模块 | 掌握度 | 说明 |
|---|---|---|
| HTML/CSS/JS 职责 | ⚠️ 半熟 | 第 1 题：把 HTML 说成"展示"，和 CSS 混了 |
| `querySelector` / DOM 操作 | ✅ 扎实 | |
| `event.preventDefault()` | ✅ 扎实 | 原因说清了 |
| `fetch` 返回值 / `response.json()` | ⚠️ 半熟 | **第 7 题错**：和 `response.ok` 搞混了 |
| `response.ok` 的作用 | ❌ 缺失 | 见第二章，重点 |
| `JSON.stringify` / `Content-Type` | ⚠️ 半熟 | 第 9 题方向反了（是**后端**解析失败） |
| 404 会不会让 fetch 抛异常 | ✅ 扎实 | 第 6 题答对 |
| 同源 / 跨域 / CORS | ✅ 扎实 | 第 10–12 题全对，今天最难的几题 |
| Console vs Network 分工 | ✅ 扎实 | 第 13 题概括准确 |
| 204 的语义 | ✅ 扎实 | 第 16 题答对 |
| 二次确认 / 双层校验 | ⚠️ 半熟 | 第 17、18 题**太简略**，漏关键点 |
| 排障理念（现象+事实） | ✅ 扎实 | 第 19 题答得好 |
| **联调排查顺序** | ❌ 缺失 | 第 20 题把"数据流"当成了"排查顺序"，考核重点 |
| 4 个 TODO | ✅ 独立完成 | |
| 5 处故障注入 | ✅ 独立完成 | |

---

# 第一章 前端三件套

| 技术 | 管什么 | 关键词 |
|---|---|---|
| HTML | **结构** | 页面上**有什么**（表单、列表、按钮） |
| CSS | **样式** | **长什么样**（布局、颜色、划线） |
| JavaScript | **行为** | **发生什么**（监听事件、发请求、改 DOM） |

> ⚠️ **第 1 题的问题**：你把 HTML 说成"界面展示"、CSS 说成"界面展示样式"——两者都落在"展示"上，等于没分开。
> 改正：**HTML = 结构（骨架），CSS = 样式（皮肤），JS = 行为（肌肉）**。HTML 不负责"好看"，那是 CSS 的事。

```javascript
const form = document.querySelector("#task-form");          // 找元素（第一个匹配的，找不到返回 null）
const btn  = form.querySelector('button[type="submit"]');    // 可以在元素内部找
taskList.innerHTML = "";                                     // 清空
li.appendChild(actions);                                     // 重建
title.textContent = task.title;                              // 用 textContent，不用 innerHTML（防注入）
```

---

# 第二章 ⚠️ 弱项 B：`response.ok` ≠ `response.json()`

**这是第 7 题的失分点。** 你把 `response.json()` 说成"检查网络通了但业务是否成功的方法"——它**不检查任何东西**。

| | 作用 | 类比 |
|---|---|---|
| `response.ok` | **判断**成功/失败（status 是否在 200–299） | **质检员**：这批货合格吗？ |
| `response.json()` | **解析** body，把文本转成 JS 对象 | **拆箱工**：把箱子打开取出东西 |

关键认知：

- `response.json()` **是纯数据加工，不做判断**。就算后端返回 500，它照样会把错误 body 解析出来——今天我们那个 `[object Object]` 就是这么来的（它老老实实把 422 的错误详情解析成了数组）。
- `fetch` **不会**因为 4xx/5xx 抛异常（第 6 题你答对了）。所以"判断成功"这件事**只能靠 `response.ok` 手写**。

```javascript
const response = await fetch(url, options);

if (!response.ok) {          // ← 质检员：这里判断成败
    throw new Error(...);
}

return response.json();      // ← 拆箱工：这里只负责解析
```

> 记忆锚点：**`ok` 管"行不行"，`json()` 管"是什么"。**

---

# 第三章 ⚠️ 弱项 A：「没响应」≠「响应说没有」

**这是第 14、15 两题连续失分的地方**——同一个错误模型暴露了两次。你两次都写了"服务器**没有响应**"来描述 404。**这是错的。**

> **404 本身就是一个完整的响应。**

服务器不仅收到了请求，还认真处理并回了一整句话："你要的 `/task` 我这没有。" 它有状态码、有响应头、有 body（`{"detail":"Not Found"}`）。这是 HTTP 层一次**成功的对话**。

| | 没响应（网络层失败） | 响应说没有（HTTP 404） |
|---|---|---|
| 请求到服务器了吗 | ❌ 没到 | ✅ 到了 |
| 服务器回话了吗 | ❌ 没有 | ✅ 回了，还回了完整一份 |
| Network 的 Status | **空白 / (failed)** | **404（有数字）** |
| Console | `Failed to fetch` + `net::ERR_*` | **通常没有报错** |
| 常见原因 | 服务没开、端口/地址错、CORS | 路径写错、资源/ID 不存在 |

> **判据一句话：Status 列有没有数字。**
> 有数字 = 服务器响应了（哪怕它说的是"没有"）；空白 = 压根没响应。

**顺带修正第 15 题的另外两处：**

- `Failed to fetch` 不是"js 代码层的错误"——它是**网络层失败**被 JS 感知到的异常。代码本身没写错。
- 能产生 `Failed to fetch` 的原因有**三种**：① 服务没开 ② 端口/地址错 ③ **被 CORS 拦**。你漏了第三种。

---

# 第四章 CORS（你掌握得很好，只做巩固）

> **CORS 是浏览器执行的限制，不是 FastAPI 执行的。**

- 浏览器有**同源策略**（安全机制）：防止你打开的恶意页面偷偷用你的登录态去请求别的网站。
- **同源 = 协议 + 主机 + 端口，三者完全一致。** `127.0.0.1:5500` 与 `127.0.0.1:8000` 端口不同 → 跨域。
- `localhost` 和 `127.0.0.1` 是**两个不同的 origin**，所以 `allow_origins` 两个都写。
- 后端（FastAPI）只是通过响应头 `Access-Control-Allow-Origin` 说一句"**我允许这个来源**"，**是否放行由浏览器决定**。
- **curl 不受限制**，因为它不是浏览器，不执行同源策略。

**最反直觉的一点**（故障 4 实测）：

```
net::ERR_FAILED 200 (OK)
```

请求到达了、后端正常返回了 **200**、body 都生成好了（`content-length: 666`），但浏览器**在把结果交给 JS 之前把它扣下了**。

> 所以"浏览器失败"发生在**第三步**——交付环节，不是请求环节也不是处理环节。

---

# 第五章 204 与「前端报错 ≠ 操作失败」

**204 No Content = 服务器成功处理了请求，但没有内容需要返回。** 规范要求它**不带 body**。

```javascript
if (response.status === 204) {
    return null;    // 无 body，不能解析 JSON
}

return response.json();
```

两个易错点：

1. **`response.ok` 对 204 是 `true`**（2xx），所以错误分支**拦不住它**，必须显式判断。
2. 不处理的话，对空 body 调 `.json()` → `SyntaxError: Unexpected end of JSON input`。

**故障 5 的完整教训链**（很值得记）：
```
DELETE 成功 → 返回 204（无 body）
  → 前端 .json() 崩了 → 页面显示"删除失败"
  → request() 抛异常 → removeTask 里的 loadTasks() 被跳过
  → 列表没刷新，已删除的任务还挂在页面上
  → 用户以为没删掉，再点一次 → 404
```
**一个解析 bug，滚成了一串误操作。前端报错 ≠ 操作失败——数据其实已经删了。**

---

# 第六章 ⚠️ 弱项 C：排查顺序 ≠ 数据流顺序

**这是第 20 题的失分点，也是考核「联调排障」20 分的核心。**

你答的是：
> 请求发出 → 网络层 → 后端处理 → 返回数据 → 前端 JS 处理 → DOM 渲染

这是**一次正常请求的生命周期**（数据流），不是**出问题时的排查动作**。

**排查顺序的本质是「分叉判断」——第一眼就把问题劈成两半：**

```text
① 看 Network 的 Status —— 第一个动作，决定往哪查
   ├─ 空白 / (failed) → 网络层：服务启了吗？端口/地址对吗？被 CORS 拦了吗？
   └─ 有数字          → HTTP 层：404 路径错 / 422 body 或校验 / 400 业务校验 / 500 服务端异常

② 看 Console 报错原文 + 文件名行号（Console 给"哪一行"，Network 给"请求怎么了"）

③ 看请求细节：URL、Method、Request Headers(Content-Type)、Payload、Response body

④ 用 curl 绕过浏览器验证后端 —— 区分"是后端问题还是浏览器问题"

⑤ 定位 → 说明原因 → 修复 → Ctrl+F5 验证
```

> 区别记住：**你的答案是"顺着一遍走"，排查是"先分叉再深入"。**

---

# 第七章 五处故障速查表（你已亲手复现）

| # | 故障 | Console | Network Status | 请求到服务端？ | 服务端返回了？ |
|---|---|---|---|---|---|
| 1 | 端口写错 | `ERR_CONNECTION_REFUSED` | **空白 / (failed)** | ❌ | ❌ |
| 2 | 路径写错 | **无 JS 报错** | **404** | ✅ | ✅ 404 |
| 3 | 丢 `Content-Type` | 无 | **422** | ✅ | ✅ 422（detail 是数组） |
| 4 | 拆 CORS 中间件 | `CORS policy` + `ERR_FAILED` | **200** | ✅ | ✅ 200 |
| 5 | 204 强行 `.json()` | `Unexpected end of JSON input` | **204** | ✅ | ✅ 204（无 body） |

**故障 3 的补充**：不写 `Content-Type` → `fetch` 自动补 `text/plain;charset=UTF-8` → FastAPI 不按 JSON 解析 → Pydantic 拿不到 `payload` → 422。
（第 9 题你答成"对**后端数据**解析失败"，方向反了：是**后端无法解析你的请求体**。）

**贯穿主线**：`Failed to fetch` 一句话背后至少有**三种**根因（服务没开 / 端口错 / CORS）→ **判据永远是 Network 的 Status，而不是 Console 的文案。**

---

# 第八章 答题要补的「另一半」

第 17、18 题你答得太简略，漏了关键点。这类"为什么"题，**要答到两层**：

**17. 删除为什么要二次确认？**
> 你答："危险操作。" ✅ 方向对，但太短。
> 完整：删除**不可逆**，误点一下数据就没了；二次确认（`window.confirm`）用**极低成本**给用户一个**反悔的机会**。

**18. 前端已校验，后端为什么还要校验？**
> 你答："避免无用请求打到后端。" ✅ 但这只是**一半**。
> 完整：**前端的校验可以被绕过**——用户改 JS、删掉 `required` 属性、或者直接用 curl 打接口，前端根本拦不住。所以：
> **前端校验管体验（即时反馈、省流量），后端校验管安全（最后一道防线）。后端永远不能信任前端传来的数据。**

---

# 第九章 今日高频坑（实操踩过的）

| 坑 | 原因 | 处理 |
|---|---|---|
| `preventDefault()` 看起来"没用" | 输入框有 `required` 且为空时，浏览器**先**拦截，根本不触发 submit 事件 | 用受控 A/B 实验区分：先填内容再对比 |
| 模板字符串被换行断开 | 反引号内**真实换行会保留进字符串** | 写一行或用 `+` 拼接（HTML 会把换行折叠成空格，所以这次没暴露） |
| 页面提示 `[object Object]` | 422 的 `detail` 是**数组**，`??` 只对 null/undefined 兜底 → `new Error(数组)` 字符串化 | 把 `loc` + `msg` 提取成人话 |
| `chrome-extension://` 报错 | 浏览器插件噪音，与本人页面无关 | 在 Console 按来源过滤 |
| 新终端 venv 自动切成别的环境 | IDE 集成终端自动激活 | 先 `deactivate`，再 `uv run`；长期方案：指定项目 `.venv` 解释器 |

---

# 第十章 20 题自测速查

| # | 答案（精简） |
|---|---|
| 1 | HTML 结构 / CSS 样式 / JS 行为 |
| 2 | 用选择器查**第一个**匹配元素，找不到返回 null |
| 3 | 阻止表单默认行为（刷新/跳转），改用 fetch 异步提交 |
| 4 | `Response` 对象 |
| 5 | 把 body 文本**解析**成 JS 对象（纯加工，不判断） |
| 6 | 不会。404 是合法响应，fetch 照常 resolve；只有网络层失败才 reject |
| 7 | 因为 fetch 不会对 4xx/5xx 报错，不检查就会把错误当成功 |
| 8 | body 只接受字符串，需把对象序列化成 JSON 文本 |
| 9 | 告诉服务器 body 是 JSON；不写则默认 `text/plain` → 后端不解析 → 422 |
| 10 | 同源 = 协议+主机+端口全同；5500 ≠ 8000 |
| 11 | **浏览器**执行限制；后端只是发许可 |
| 12 | curl 不是浏览器，不执行同源策略 |
| 13 | Console 看 JS 报错（含行号）；Network 看请求本身（状态码/头/body） |
| 14 | **到达了**，且服务器明确回了 404 |
| 15 | 网络层（Status 空白，可能没到）vs HTTP 层（Status 404，到了） |
| 16 | 204 语义是"成功但无 body"，解析会抛错；且 ok 为 true，拦不住 |
| 17 | 删除不可逆，确认给用户反悔机会 |
| 18 | 前端管体验，后端管安全（前端校验可被绕过） |
| 19 | 缩小问题空间，避免 AI 猜、避免反复沟通 |
| 20 | ①Status 分叉 ②Console ③请求细节 ④curl 对照 ⑤定位→说因→修复→验证 |

---

# 第十一章 今日完成标准（自检）

不看讲义，能独立做到：

```text
用 HTML/CSS/JS 写出任务管理页面
+ fetch 完成 CRUD 四个操作 + 状态过滤 + 统计展示
+ 说清 response.ok / response.json() / 204 / CORS 各自的作用
+ 遇到联调问题，能按"Status 分叉"的顺序定位并说明根因
```

**下一步**：Day 3 考核（100 分，80 及格，**联调排障占 20 分**）。先把第二、三、六章的三处概念翻转确认一遍，再进考核。

---

*本文档由 Claude 根据你 2026-09-10 的实操与 2026-09-11 的自测结果生成。重点看第二、三、六章的 ⚠️ 专项。*
