# Day 3 学习日志：前端与后端 HTTP 联调

> **日期**：2026-09-10
> **主题**：前端三件套 + fetch 联调 + CORS + 联调排障
> **仓库**：`vibe-training-practice`（前端 `frontend/`，后端 `backend/`）

---

## 一、今日目标

把 Day 2 写好的 FastAPI 任务 API 接到浏览器页面上，跑通完整的增删改查联调，并掌握"前端报错 → 定位根因"的排障方法。

---

## 二、完成内容

### 1. 前端三件套职责划分

| 技术         | 管什么    | 今天的具体载体                                                          |
| ---------- | ------ | ---------------------------------------------------------------- |
| HTML       | **结构** | 表单 `#task-form`、下拉框 `#status-filter`、列表 `#task-list`、统计 `#stats` |
| CSS        | **样式** | `.task-item` 布局、`.task-done` 完成态划线                               |
| JavaScript | **行为** | 事件监听、DOM 重建、发请求、更新界面                                             |

### 2. 打通 fetch 联调

封装了统一的 `request()`，集中处理错误、`response.ok` 判断、204 特判、JSON 解析，避免每个调用点各写一遍。

| 操作     | 请求                         | 后端行为 |
| ------ | -------------------------- | ---- |
| 加载列表   | `GET /tasks`（带过滤 Query 参数） | 200  |
| 新增任务   | `POST /tasks`              | 201  |
| 切换完成状态 | `PUT /tasks/{id}`          | 200  |
| 删除任务   | `DELETE /tasks/{id}`       | 204  |
| 加载统计   | `GET /stats`               | 200  |

### 3. 后端 CORS 配置

在 `backend/app/main.py` 加 `CORSMiddleware`，`allow_origins` 放行前端来源（`http://127.0.0.1:5500` 与 `http://localhost:5500` 都写了，因为二者是**不同的 origin**）。

### 4. 四个 TODO 全部实现

| TODO    | 实现要点                                                                                    |
| ------- | --------------------------------------------------------------------------------------- |
| 状态过滤下拉框 | `buildTaskListPath()` 按 `all / pending / completed` 拼 Query 参数 `completed=`             |
| 删除二次确认  | `window.confirm`，取消则直接 `return`，不发 DELETE                                               |
| 提交时禁用按钮 | `submitButton.disabled = true`，并用 `try...finally` 保证即使异常也能恢复                            |
| 统计信息展示  | 调 `/stats`，显示"总任务 X \| 已完成 Y \| 未完成 Z"；挂在 `loadTasks()` 的 try/catch **之后**统一刷新，避免增删改后遗漏 |

### 5. 五处故障注入（全部亲手复现 + 修复）

| #   | 故障                    | 现象                                         | 结论                                         |
| --- | --------------------- | ------------------------------------------ | ------------------------------------------ |
| 1   | 后端端口写错                | `ERR_CONNECTION_REFUSED`，Network Status 空白 | 请求**没到**服务端                                |
| 2   | API 路径写错              | 404，**无 JS 报错**                            | 到了，服务器说"没这个路径"                             |
| 3   | POST 丢 `Content-Type` | 422 + 页面显示 `[object Object]`               | fetch 默认发 `text/plain`；错误处理假设了 detail 是字符串 |
| 4   | 拆掉 CORS 中间件           | CORS 报错 + `ERR_FAILED **200**`             | 请求成功、响应成功，**浏览器拒绝把结果交给 JS**                |
| 5   | 204 强行 `.json()`      | `Unexpected end of JSON input`             | 204 无响应体；**前端解析失败 ≠ 操作失败**                 |

---

## 三、关键收获（认知层面）

1. **`Failed to fetch` 一句话背后至少三种根因**（服务没开 / 端口错 / CORS）。判据不是 Console 的文案，而是 **Network 的 Status 列**——有数字说明请求到了，空白说明没到。

2. **CORS 是浏览器的策略，不是 HTTP 协议规则。** 所以 curl 能成功、浏览器会失败——请求和响应都正常，被拦的是"交付给 JS"这一步。同源 = 协议 + 主机 + 端口，三者全同。

3. **前端报错 ≠ 操作失败。** 故障 5 里 DELETE 已经成功（204），只是前端解析空 body 时崩了；因为抛异常，后续的 `loadTasks()` 被跳过，列表没刷新，陈旧 UI 又引诱用户重复点击，撞出第二个 404。**一个解析 bug 滚成一串误操作。**

4. **204 = 服务器成功处理，但没有内容需要返回。** 必须显式识别，不能无脑 `.json()`。注意 `response.ok` 对 204 是 `true`（2xx），所以错误分支拦不住它。

5. **错误处理不能假设响应结构统一。** FastAPI 的 400/404 返回 `detail` 是字符串，而 422 返回的是**数组**；`??` 只对 `null/undefined` 兜底，数组会原样透传，`new Error(数组)` 就渲染成 `[object Object]`。给**人**看的错误提示，要把 `loc` + `msg` 提取出来。

6. **"无 JS 报错"是设计出来的，不是运气。** 故障 2 里之所以没有未捕获异常，是因为 `request()` 抛出后由调用方的 `try/catch` 接住了。

---

## 四、遇到的问题与解决

| 问题                                | 原因                                      | 解决                                               |
| --------------------------------- | --------------------------------------- | ------------------------------------------------ |
| 新终端里 `uvicorn` 不被识别，venv 自动切成别的环境 | IDE 集成终端自动激活了其他虚拟环境                     | 先 `deactivate`，再 `uv run`；长期方案是给项目指定 `.venv` 解释器 |
| 模板字符串被换行断开                        | 反引号内的真实换行会保留进字符串                        | 改成一行或用 `+` 拼接（本次被 HTML 空白折叠掩盖，属侥幸正确，需修）          |
| 新增任务页面提示 `[object Object]`        | 422 的 `detail` 是数组，`new Error(数组)` 字符串化 | 计划加固 `request()`：数组型 `detail` 拼成 `loc: msg` 可读文本 |
| `preventDefault()` 看起来"没用"        | 输入框有 `required`，空标题时表单根本不触发 submit      | 用受控的 A/B 实验定位：先填内容再对比                            |
| 页面上出现 `chrome-extension://` 报错    | 浏览器插件的噪音，与本人页面无关                        | 在 Console 按来源过滤，忽略非本页面的报错                        |

---

## 五、待办 / 下一步

- [ ] Day 3 自测 20 题（讲义第十三章）

- [ ] 加固 `request()` 的 422 错误提示（`loc: msg` 可读化）
- [ ] 统一前端代码缩进（VS Code 格式化）
- [ ] 提交本次改动到 git，并补一份 Day 3 复习文档
- [ ] 学习day4内容

---

## 六、一句话总结

> 今天最大的收获不是"页面能跑了"，而是**建立了排障判据**：遇到前端报错，先看 Network 的 Status，把"请求有没有到服务端""服务端有没有正常返回""浏览器有没有把结果交给 JS"这三层拆开，就不会在各种长得一样的 `Failed to fetch` 里瞎改代码。

---

*本文档由 Claude 协助整理，内容基于 2026-09-10 的实操过程。*
